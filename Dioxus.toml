[application]
# Note: Use `dx serve -p planner-app` from workspace root
# The sub_package field is not supported in dx 0.7

[web.app]
# HTML title tag content
title = "planner"
base_path = "/planner"

[desktop.app]
# Desktop window configuration
title = "Quarterly Planner"

[desktop.app.window]
width = 1440
height = 900
resizable = true

# include `assets` in web platform
[web.resource]
# Additional CSS style files
style = []
# Additional JavaScript files
script = []

[web.resource.dev]
# Javascript code file
# serve: [dev-server] only
script = []
