#!/bin/sh

# Dioxus Quarterly Planner pre-commit hook to mirror CI checks.

set -eu

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

echo "→ Running cargo fmt -- --check"
cargo fmt -- --check

echo "→ Running cargo clippy (web target)"
cargo clippy --target wasm32-unknown-unknown --features web -- -D warnings

echo "→ Running cargo clippy (desktop target)"
cargo clippy --features desktop --all-targets -- -D warnings

echo "→ Running cargo test --verbose --features desktop"
cargo test --verbose --features desktop

echo "→ Running cargo doc --no-deps"
cargo doc --no-deps

if ! command -v cargo-audit >/dev/null 2>&1; then
  cat <<'EOF' >&2
🚫 Missing required tooling: cargo-audit.
Install it once with:
  cargo install cargo-audit --locked
EOF
  exit 1
fi

echo "→ Running cargo audit"
cargo audit

if ! command -v dx >/dev/null 2>&1; then
  cat <<'EOF' >&2
🚫 Missing required tooling: dioxus-cli (dx).
Install it once with:
  cargo install dioxus-cli --locked
EOF
  exit 1
fi

echo "→ Running dx bundle --release (WASM build)"
dx bundle --release

echo "✓ All pre-commit checks passed"
